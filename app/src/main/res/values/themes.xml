<resources>
    <!-- Tema dell’app: eredita un M3 DayNight con NoActionBar già integrato -->
    <style name="Theme.AppBuilder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Esempi opzionali, non essenziali per il linking -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>