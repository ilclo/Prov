<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sfondo -->
    <item android:drawable="@color/ic_launcher_background"/>
    <!-- Foreground (il vector) -->
    <item android:drawable="@drawable/ic_launcher_foreground"/>
</layer-list>
